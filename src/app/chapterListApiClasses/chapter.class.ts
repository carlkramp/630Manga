export class Chapter {
  chapter: string;
  count: number;
  id: string;

  constructor(chapter: string, count: number, id: string) {
    this.chapter = chapter;
    this.count = count; 
    this.id = id;
  }
}
